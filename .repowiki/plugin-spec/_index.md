# @repowiki/plugin-spec

> Path: `plugin-spec`

## Overview
The `plugin-spec` package in the TypeScript project defines the foundational interfaces and types for plugin specifications. It includes the `src` directory, which contains the core implementation and definitions used by plugins. This package serves as a reference and contract for developing and integrating plugins within the system.

## Children
- [src](./src/_index.md)
