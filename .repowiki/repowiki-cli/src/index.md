# index

> Path: `repowiki-cli/src/index`

## Overview
The `repowiki-cli/src/index` module exports a single constant, `VERSION`, which represents the current version of the CLI tool. It serves as a central location for version information used throughout the application. This module is typically imported to display or check the version number in CLI output or logs.

## Exports
- `const VERSION`
