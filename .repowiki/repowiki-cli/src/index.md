# index

> Path: `repowiki-cli/src/index`

## Overview
The `repowiki-cli/src/index` module exports a single constant `VERSION`, which holds the current version number of the CLI tool. This file serves as the entry point for the application and is used to provide version information. It does not include any other functions or classes.

## Exports
- `const VERSION`
