# index

> Path: `repowiki-cli/src/index`

## Overview
The `repowiki-cli/src/index` module exports a single constant, `VERSION`, which represents the current version of the CLI tool. This file serves as the entry point for the application, providing version information to other parts of the codebase. It does not include any additional functions or classes.

## Exports
- `const VERSION`
