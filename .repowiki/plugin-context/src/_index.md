# src

> Path: `plugin-context/src`

## Overview
The `plugin-context` directory in the TypeScript project provides the core functionality for managing plugin contexts. It includes an `index` module that serves as the entry point for accessing plugin context features. The `context` subdirectory contains class definitions that handle various aspects of the plugin context implementation.

## Children
- [index](./index.md)
- [context](./commands/context/_index.md)
