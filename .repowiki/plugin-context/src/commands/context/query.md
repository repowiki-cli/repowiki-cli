# query

> Path: `plugin-context/src/commands/context/query`

## Overview
The `ContextQuery` class in the `plugin-context/src/commands/context/query` module is responsible for handling queries related to the plugin context. It provides methods to retrieve and manipulate context data within the application. This module serves as a central point for accessing contextual information used by various parts of the plugin system.

## Exports
- `class ContextQuery`
