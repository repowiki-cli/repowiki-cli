# query

> Path: `plugin-context/src/commands/context/query`

## Overview
The `ContextQuery` class in the `plugin-context/src/commands/context/query` module is responsible for handling queries related to the plugin context. It provides methods to retrieve and manipulate context data within the plugin's execution environment. This class is designed to be used within command-line operations that require access to contextual information.

## Exports
- `class ContextQuery`
