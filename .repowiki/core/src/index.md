# index

> Path: `core/src/index`

## Overview
The `core/src/index` module defines a set of core interfaces used throughout the application. It includes types for chat messages, wiki nodes, LLM providers, output backends, analyzers, and configuration options. These interfaces serve as the foundation for interacting with various components of the system.

## Exports
- `interface ChatMessage`
- `interface WikiNode`
- `interface LLMProvider`
- `interface OutputBackend`
- `interface Analyzer`
- `interface RepowikiConfig`
