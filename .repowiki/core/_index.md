# @repowiki/core

> Path: `core`

## Overview
The `node` package in the TypeScript project serves as a foundational module, with its `index` child defining core interfaces used across the application. These interfaces provide a consistent type structure for other parts of the codebase. The `core/src/index` module is essential for maintaining type integrity and interoperability within the application.

## Children
- [index](./src/index.md)
