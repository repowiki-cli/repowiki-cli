# AzureOpenAIProvider

> Path: `plugin-wiki/src/providers/AzureOpenAIProvider`

## Overview
The `AzureOpenAIProvider` class in the `plugin-wiki/src/providers/AzureOpenAIProvider` module serves as a provider for integrating with Azure OpenAI services. It handles the configuration and communication with Azure's API endpoints for AI model interactions. This class is designed to be used within the plugin-wiki framework to enable AI-powered features through Azure's machine learning capabilities.

## Exports
- `class AzureOpenAIProvider`
