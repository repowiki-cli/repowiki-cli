# OpenAIProvider

> Path: `plugin-wiki/src/providers/OpenAIProvider`

## Overview
The `OpenAIProvider` class in the `plugin-wiki/src/providers/OpenAIProvider` module serves as a wrapper for interacting with OpenAI's API. It provides methods to send requests and handle responses from OpenAI services, such as generating text or embeddings. This class is designed to be integrated into a larger application that leverages AI capabilities through OpenAI's models.

## Exports
- `class OpenAIProvider`
