# update

> Path: `plugin-wiki/src/commands/wiki/update`

## Overview
The `WikiUpdate` class in the `plugin-wiki/src/commands/wiki/update` module is responsible for handling the update functionality of wiki pages within the application. It likely provides methods to modify existing wiki content, manage revisions, and ensure data consistency. This class is part of the command structure used by the wiki plugin to process update operations.

## Exports
- `class WikiUpdate`
