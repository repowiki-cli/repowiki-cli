# update

> Path: `plugin-wiki/src/commands/wiki/update`

## Overview
The `WikiUpdate` class in the `plugin-wiki/src/commands/wiki/update` module is responsible for handling the logic related to updating wiki content. It provides methods to modify existing wiki pages, including parsing input data and applying changes to the underlying data structure. This class is part of the command system used within the plugin-wiki module.

## Exports
- `class WikiUpdate`
