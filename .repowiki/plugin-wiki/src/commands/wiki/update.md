# update

> Path: `plugin-wiki/src/commands/wiki/update`

## Overview
The `WikiUpdate` class in the `plugin-wiki/src/commands/wiki/update` module is responsible for handling the update functionality of wiki pages. It provides methods to modify existing wiki content, manage versioning, and apply changes to the underlying data store. This class is part of the command structure used within the plugin-wiki module to execute wiki-related operations.

## Exports
- `class WikiUpdate`
