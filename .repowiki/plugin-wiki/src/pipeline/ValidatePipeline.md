# ValidatePipeline

> Path: `plugin-wiki/src/pipeline/ValidatePipeline`

## Overview
The `ValidatePipeline` class in the `plugin-wiki/src/pipeline/ValidatePipeline` module is responsible for validating the structure and content of a pipeline configuration. It ensures that all required fields are present and conform to expected types and formats. This class is used to enforce consistency and correctness during pipeline setup.

## Exports
- `class ValidatePipeline`
