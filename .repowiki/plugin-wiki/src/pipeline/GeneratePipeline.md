# GeneratePipeline

> Path: `plugin-wiki/src/pipeline/GeneratePipeline`

## Overview
The `GeneratePipeline` class in the `plugin-wiki/src/pipeline/GeneratePipeline` module is responsible for orchestrating the generation process within the wiki plugin. It likely manages the flow of data and operations needed to produce wiki content. The class provides a structured approach to executing pipeline stages, ensuring consistency and modularity in the generation workflow.

## Exports
- `class GeneratePipeline`
