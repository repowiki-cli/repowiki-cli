# TypeScriptAnalyzer

> Path: `plugin-wiki/src/analyzers/typescript/TypeScriptAnalyzer`

## Overview
The `TypeScriptAnalyzer` class is a module within the `plugin-wiki` package, designed to analyze TypeScript code. It provides functionality for parsing and inspecting TypeScript files, extracting structural and semantic information. This analyzer is used to support features such as documentation generation and code navigation within the wiki plugin.

## Exports
- `class TypeScriptAnalyzer`
