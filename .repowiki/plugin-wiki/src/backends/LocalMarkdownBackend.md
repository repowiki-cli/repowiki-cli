# LocalMarkdownBackend

> Path: `plugin-wiki/src/backends/LocalMarkdownBackend`

## Overview
The `LocalMarkdownBackend` class in the `plugin-wiki/src/backends/LocalMarkdownBackend` module is responsible for handling local Markdown file operations within the wiki plugin. It provides methods for reading, writing, and managing Markdown content stored locally. This backend is typically used in environments where content is stored directly on the file system rather than a remote server.

## Exports
- `class LocalMarkdownBackend`
